#include<stdio.h>
int fact(int a)
{
    if(a==1)
    return 1;
    
    else
    return (a*fact(a-1));
}
void main()
{
 int n,d;
 scanf("%d",&n);
 d=fact(n);
 printf("%d is the factorial of %d",d,n);
}
